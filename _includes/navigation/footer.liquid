<nav aria-label="Footer">
    <ul>
        {%- for nav in footer %}
        <li><a{% if url==nav.url %} aria-current="page"{% endif %} href="{{ nav.url | url }}">{{ nav.data.title }}</a></li>
        {%- endfor %}
    </ul>
</nav>